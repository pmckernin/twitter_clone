<%= form_with(model: @follow, local: true) do |f| %>

  <%= f.hidden_field :follower_id, value: @user.id %>

  <div class="form-group">
    <%= f.label :following_id %>

    <%= f.text_field :following_id, class: "form-control" %>
  </div>

  <%= f.submit "Create follow", class: "btn btn-block btn-outline-secondary" %>
<% end %>
